import random

#list = ["apple", "cherry", "strawberry", "pear"]
mask = []
#random_word = list[random.randrange (0, len(list)-1)]

counter = 0

random_word = "apple"

for n in random_word:
        mask.append("_")